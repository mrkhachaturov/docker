---
tunnel: $CLOUDFLARE_MR_TUNNEL_ID
credentialsFile: /etc/cloudflared/cloudflare-tunnel.json
originRequest:
  originServerName: external.${DOMAINNAME_MR}
  
ingress:
  - hostname: ${DOMAINNAME_MR}
    service: https://traefik-ext
  - hostname: "*.${DOMAINNAME_MR}"
    service: https://traefik-ext
  - service: http_status:404
